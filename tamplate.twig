<{{ moduleConfig.levelHeader}}
 class="header header_{{ moduleConfig.levelHeader }} {% if underline %} header_underline {% endif %}">

    <span {% if underline %} class="header_highlight" {% endif %}>
    	{{ moduleConfig.textHeader }}
    </span>
    
 </{{ moduleConfig.levelHeader}}>
 
 
 {# Logo #}
 
 {% if moduleConfig.showLogo != 'none' %}
    {% if moduleConfig.showLogo == 'image' and moduleConfig.image %}
        <figure class="logo">
            <a href="{{ shopUrls.mainPageUrl }}">
                {{
                    image({
                        img: {
                            src: moduleConfig.image.paths.original,
                            width: moduleConfig.image.width,
                            height: moduleConfig.image.height,
                            alt: shopInfo.name ? : "",
                            title: shopInfo.name,
                            class: 'logo',
                            fetchpriority: 'high'
                        }
                    },
                    [
                        {
                            src: moduleConfig.image.paths.original,
                            type: moduleConfig.image.format
                        }
                    ]
                )}}
            </a>
        </figure>
    {% elseif moduleConfig.textLogoInput %}
        <h5> {{ moduleConfig.textLogoInput }} </h5>
    {% else %}
        <h5>{{ shopInfo.name }}</h5>
    {% endif %}
{% endif %}

{# Telefon i godziny #}

